setup:
    addons:
        - plan: heroku-postgresql
        - plan: heroku-redis
build:
    docker:
        web: sample/Dockerfile
release:
    image: web
    command:
        - python manage.py migrate
